[pytest]
addopts = --import-mode=prepend
# Registro de marks usados no projeto
markers =
    integration: marks tests as integration tests (may be skipped on CI)
    slow: marks slow tests
# Recomendo rodar com -m "not integration and not slow" nos runs rápidos
