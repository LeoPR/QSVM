[pytest]
testpaths = tests
python_files = test_*.py
markers =
    integration: marks tests as integration (network, slow, downloads)
    slow: marks slow-running tests
addopts = -q --maxfail=1
filterwarnings =
    ignore::DeprecationWarning